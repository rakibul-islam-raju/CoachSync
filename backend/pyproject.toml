[project]
name = "coaching-management-backend"
version = "0.1.0"
description = "Backend API for the Coaching Management System."
requires-python = ">=3.14,<3.15"
dependencies = [
    "celery",
    "django",
    "django-celery-results",
    "django-cors-headers",
    "django-environ",
    "django-filter",
    "djangorestframework",
    "djangorestframework-simplejwt",
    "drf-spectacular",
    "faker",
    "gunicorn>=26.0.0",
    "psycopg[binary]>=3.3.4",
    "redis",
    "whitenoise>=6.12.0",
]

[tool.uv]
package = false
